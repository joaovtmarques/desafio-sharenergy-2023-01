/* eslint-disable no-unused-vars */
export enum HttpStatusCode {
  OK = 200,
  CREATED = 201,
  BAD_REQUEST = 400,
  ALREADY_EXISTS = 409,
  TOKEN_EXPIRED = 410,
  NOT_PROVIDED = 422,
  NOT_FOUND = 404,
  INTERNAL_SERVER = 500,
}
